import { type ImageAsset } from '../utils/types';

export const images: ImageAsset[] = [];

// const chipSprite = chipSpriteConfig.map((sprite) => {
//   sprite.path = 'assets/spritesheets/chips.png';
//   return sprite;
// });

// export const spritesheets: SpriteSheetAsset[] = [...chipSprite];

export const allAssets = { images };
